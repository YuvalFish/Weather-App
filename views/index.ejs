<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/main.css">

    <title>Weather</title>
</head>
<body>
    <div class="main">

        <div class="search">
            <form action="/check" method="post" class="city-form">
                <input id="city-search" type="text" class="city-search" name="city" placeholder="City name" aria-placeholder="City name" required> 
                <label for="city-search" class="city-label">City name</label>
            </form>
        </div>

        <div class="hero">
            <h1 class="city-title">CITY NAME</h1>
            <h3 class="degrees">It is currently TEMP degrees outside</h3>
            <p class="emoji">🌤️</p>
        </div>

        <footer class="footer">
            <p>Yuval Fishbine © <%= new Date().getFullYear() %></p>
        </footer>
    </div>
</body>
</html>